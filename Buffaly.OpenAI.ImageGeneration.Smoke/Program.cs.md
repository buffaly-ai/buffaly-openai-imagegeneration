# Program.cs Change History

## Add service runtime facade tests (2026-04-26)
- Updated `--facade-tests` to validate initialize-time service runtime configuration, create/edit request validation, output path construction, and model/output-format normalization without requiring a live OpenAI API key.
